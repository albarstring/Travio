/**
 * Repository Exports
 * Centralized import untuk semua repositories
 * 
 * Gunakan:
 * import { userRepository, courseRepository } from "@/lib/repositories"
 */

export { UserRepository, userRepository } from "./UserRepository"
export { CourseRepository, courseRepository } from "./CourseRepository"
export { SectionRepository, sectionRepository } from "./SectionRepository"

// Template untuk repository baru:
// export { EntityRepository, entityRepository } from "./EntityRepository"
